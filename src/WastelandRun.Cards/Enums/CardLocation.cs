namespace WastelandRun.Cards
{
    /// <summary>Where a card instance currently lives within the combat card cycle.</summary>
    public enum CardLocation
    {
        Deck,
        Hand,
        Discard,
        Exhausted
    }
}
